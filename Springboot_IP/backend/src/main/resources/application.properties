spring.application.name=baby-tracking-api

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# H2 Console
spring.h2.console.enabled=true

# Server
server.port=8080
server.servlet.context-path=/api/v1

# Logging
logging.level.root=INFO
logging.level.com.babyapp=DEBUG

# ============================================
# Oracle Database (COMMENTED - Add later when AWS is ready)
# ============================================
# spring.datasource.url=jdbc:oracle:thin:@AWS_HOST:1521:ORCL
# spring.datasource.driverClassName=oracle.jdbc.OracleDriver
# spring.datasource.username=babyapp_user
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
# spring.jpa.hibernate.ddl-auto=validate
